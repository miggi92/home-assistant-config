{
  "config": {
    "step": {
      "user": {
        "title": "MCP Webhook Proxy",
        "description": "This integration is managed by the Webhook Proxy for HA MCP add-on. It proxies MCP requests via a webhook for remote access."
      }
    },
    "abort": {
      "already_configured": "MCP Webhook Proxy is already configured."
    }
  },
  "issues": {
    "oauth_restart_required": {
      "title": "Restart Home Assistant to finish the MCP Webhook Proxy setup",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Restart Home Assistant?",
            "description": "The MCP Webhook Proxy add-on changed something Home Assistant can only apply at startup, and the running instance has not picked it up yet. Either OAuth was enabled and the OAuth-enforcing integration code is not loaded, or the webhook ID was rotated and the new URL's OAuth discovery route is not bound. Submitting this form will restart Home Assistant so the change takes effect.\n\nClick **Submit** to restart now."
          }
        }
      }
    },
    "update_restart_required": {
      "title": "Restart Home Assistant to finish the MCP Webhook Proxy update",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Restart Home Assistant?",
            "description": "The MCP Webhook Proxy integration was updated by the add-on, but Home Assistant is still running the previous version's code. Submitting this form will restart Home Assistant so the new code takes effect. The webhook keeps working with the previous version until then.\n\nClick **Submit** to restart now."
          }
        }
      }
    }
  },
  "services": {
    "refresh_repairs": {
      "name": "Refresh repairs",
      "description": "Used by the Webhook Proxy add-on to raise or clear the click-to-restart Repair issues. Not intended for manual use.",
      "fields": {
        "issue_id": {
          "name": "Issue",
          "description": "Which restart Repair issue to raise or clear."
        },
        "action": {
          "name": "Action",
          "description": "Whether to create or clear the issue."
        }
      }
    }
  }
}
