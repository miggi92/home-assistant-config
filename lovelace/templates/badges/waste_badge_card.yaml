---
waste_badge_template:
  card:
    type: custom:mushroom-template-badge
    icon: "{{ state_attr(entity, 'icon')}}"
    entity: "[[entity]]"
    content: "{{states(entity) }}"
    tap_action:
      action: more-info
