---
# Recorder
# https://www.home-assistant.io/integrations/recorder/
recorder:
  exclude:
    domains:
      - automation
      - update
    entity_globs:
      - sensor.sun*
      - weather.*
      - sensor.flightradar24*
