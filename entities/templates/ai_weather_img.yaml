---
#
- trigger:
    - alias: "Update image when a new weather image is generated"
      trigger: event
      event_type: new_weather_image
  image:
    - name: "AI generated weather image"
      unique_id: ai_generated_weather_image
      url: "http://localhost:8123{{ trigger.event.data.url }}"
